% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_visualize.R
\name{prepare_plot_data}
\alias{prepare_plot_data}
\title{Prepare Raster Data for Plotting}
\usage{
prepare_plot_data(
  before_raster,
  after_raster = NULL,
  change_mask = NULL,
  type = c("sidebyside", "overlay", "difference")
)
}
\arguments{
\item{before_raster}{A RasterLayer object representing the original (before) raster.}

\item{after_raster}{A RasterLayer object representing the modified (after) raster.}

\item{change_mask}{A RasterLayer object representing the binary change mask.}

\item{type}{A character string specifying the type of visualization. Possible values are:
\itemize{
\item "sidebyside": Converts the before and after rasters into separate columns for comparison.
\item "overlay": Merges the before raster with the change mask to show detected changes.
\item "difference": Calculates the difference between the before and after rasters.
}}
}
\value{
A data frame with columns: \code{x} (longitude), \code{y} (latitude), and \code{value} (raster value).
}
\description{
This helper function prepares raster data for visualization by converting raster objects
into a tidy data frame that can be plotted using \code{ggplot2}. It handles different types
of visualizations such as side-by-side comparisons, overlays, and differences.
}
\keyword{internal}
