% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postprocess_mask.R
\name{postprocess_mask}
\alias{postprocess_mask}
\title{Post-process Change Mask}
\usage{
postprocess_mask(
  mask,
  operation = c("closing", "opening", "clump"),
  kernel_size = 3
)
}
\arguments{
\item{mask}{A \code{SpatRaster} object representing a binary change mask (1 for change, 0 for no change).}

\item{operation}{A character string specifying the morphological operation to be applied.
Options are "closing", "opening", or "clump".}

\item{kernel_size}{An integer defining the size of the matrix (e.g., 3 for a 3x3 matrix) to be used in the morphological operations.
Default is 3.}
}
\value{
A \code{SpatRaster} object with the processed change mask.
}
\description{
This function applies morphological operations such as closing, opening, or clumping to a binary change mask.
These operations can be useful for cleaning up the change mask and removing small artifacts or grouping adjacent pixels.
}
\examples{
\dontrun{
library(terra)
# Create a sample binary mask
mask <- rast(matrix(c(0, 1, 0, 1, 0, 0, 1, 0, 0), nrow = 3))

# Apply closing operation
processed_mask <- postprocess_mask(mask, operation = "closing", kernel_size = 3)

# Apply clumping operation
clumped_mask <- postprocess_mask(mask, operation = "clump")
}

}
